# 03.05 Generator Specification

> Contract generator dan module console untuk reusable Laravel Engineering Starter Kit.

## Document Information

| Item | Value |
|---|---|
| Version | 1.1 |
 | Status | Approved |
| Owner | Software Architect |
| Audience | Developer, QA, AI Assistant |
| Depends On | `02.04-SYSTEM-DESIGN.md`, `03.01-CODING-STANDARD.md`, `03.04-FOLDER-STRUCTURE.md` |
| Related | `03.02-CONSOLE-COMMANDS.md`, `03.08-STUB-SPEC.md`, `06.05-GENERATOR-ENGINE.md`, `07.08-GENERATOR-SERVICE.md` |

## Purpose

Generator adalah jalur resmi untuk membuat skeleton module secara konsisten.
Profile `default-v1` saat ini menghasilkan manifest, runtime config, permission
source kosong, provider, README, route entry point, dan directory DDD-lite.
Generator belum menghasilkan business logic, migration, test, frontend, atau
permission final.
Module baru wajib dibuat melalui `module:make`. Pembuatan manual hanya boleh
untuk framework maintenance atau kasus khusus yang memiliki exception ADR,
review architect, dan test output yang setara generator.

## Baseline Objectives

- Membuat module pada `app/Modules/{Domain}/{Submodule}`.
- Menghasilkan `module.json`, `module.php`, dan `permissions.php`.
- Menghasilkan layer DDD-lite dan communication boundary.
- Menghasilkan route entry point dan README minimum.
- Menyediakan directory untuk migration, factory, seeder, test, dan frontend;
  file implementasinya dibuat pada tahap module.
- Menjalankan naming, dependency, conflict, dan forbidden package validation.
- Menyediakan output human-readable dan `--json`.

## Supported Baseline Commands

```bash
php artisan module:make UserManagement --domain=System --force --yes --json
php artisan module:make AccessControl --domain=System --force --yes --json
php artisan module:make AuditLog --domain=System --force --yes --json
php artisan module:make SystemSetting --domain=System --force --yes --json
php artisan module:list
php artisan module:discover
php artisan module:validate
```

Lifecycle command `module:enable` dan `module:disable` belum tersedia pada
implementasi saat ini dan tidak menjadi scope baseline generator.

CRUD, feature, resource, dan API generator belum menjadi baseline pertama.

## Generator Profiles

Profile adalah preset output generator yang versioned dan memiliki test sendiri.
Profile yang tersedia saat ini hanya `default-v1`. Profile `platform`,
`business`, `integration`, dan `minimal` masih merupakan rencana.

```bash
php artisan module:make UserManagement --domain=System --profile=default-v1 --dry-run --json
```

Profile `default-v1` menghasilkan directory DDD-lite, manifest, runtime config,
permission source kosong, provider, README, dan route entry point. Target
baseline pertama adalah `AccessControl`, lalu `UserManagement` setelah public
authorization contract tersedia.

## Generator Options

Semua generator filesystem mendukung:

- `--dry-run`: menampilkan planned changes tanpa menulis.
- `--force`: wajib untuk operasi mutasi; tidak membuka overwrite pada Phase 3.
- `--json`: machine-readable output.
- `--profile`: memilih profile output yang tersedia, saat ini `default-v1`.

Default generator fail-safe dan tidak menimpa file.

`--force` pada terminal interaktif wajib meminta confirmation. Pada CI atau
non-interactive environment, overwrite hanya boleh dilakukan dengan kombinasi
eksplisit `--force --yes`; `--force` tanpa `--yes` harus gagal.

## Input Contract

Generator menerima:

- domain/module name;
- submodule name;
- optional profile;
- optional namespace/path yang tervalidasi;
- dependency list yang terdaftar pada manifest.

Input wajib divalidasi sebelum filesystem berubah. Module name memakai PascalCase;
command argument boleh menerima kebab-case yang dinormalisasi secara eksplisit.

## Generated Backend Structure

```text
app/Modules/System/AccessControl/
|-- Application/
|   |-- Actions/
|   |-- DTO/
|   |-- Queries/
|   |-- Services/
|   `-- Contracts/
|-- Domain/
|   |-- Contracts/
|   |-- Entities/
|   |-- Events/
|   |-- Exceptions/
|   |-- Services/
|   `-- ValueObjects/
|-- Infrastructure/
|   |-- Persistence/
|   |   |-- Models/
|   |   `-- Repositories/
|   |-- Observers/
|   |-- Providers/
|   `-- External/
|-- Presentation/
|   |-- Controllers/
|   |-- Policies/
|   |-- Requests/
|   `-- Resources/
|-- Database/
|   |-- Factories/
|   |-- Migrations/
|   `-- Seeders/
|-- Routes/
|   |-- api.php
|   |-- web.php
|   |-- console.php
|   `-- channels.php
|-- Tests/
|   |-- Feature/
|   |-- Integration/
|   `-- Unit/
|-- module.json
|-- permissions.php
|-- ServiceProvider.php
`-- README.md
```

Generated structure boleh memakai profile lebih kecil, tetapi profile harus
versioned dan memiliki test output sendiri.

### Default-v1 Profile File Manifest

Profile `default-v1` menghasilkan file yang dapat di-track oleh Git. Folder
kosong tetap dibuat untuk menjaga struktur canonical. Output file saat ini:

```text
Application/Actions/
Application/Contracts/
Domain/Contracts/
Infrastructure/Providers/
Presentation/Policies/
Database/Migrations/
Tests/Unit/
Routes/api.php
Routes/web.php
module.json
module.php
permissions.php
ServiceProvider.php
README.md
```

Directory tambahan dibuat untuk menjaga golden structure. File implementasi,
test, migration, factory, seeder, dan frontend belum dibuat oleh profile ini.
Output generator harus reproducible untuk input dan profile version yang sama.

## Generated Frontend Structure

```text
resources/js/pages/System/AccessControl/
|-- components/
|-- pages/
|   |-- Index.tsx
|   |-- Create.tsx
|   |-- Edit.tsx
|   `-- Show.tsx
|-- schemas.ts
|-- types.ts
`-- index.ts
```

Module-specific component wajib berada di `components/` module. Shared component
berada di `resources/js/components/ui` atau `resources/js/components/shared`.

## Generated Metadata

### `module.json`

Minimal metadata:

```json
{
  "name": "AccessControl",
  "namespace": "App\\Modules\\System\\AccessControl",
  "version": "1.0.0",
  "schema_version": 1,
  "status": "enabled",
  "domain": "System",
  "path": "app/Modules/System/AccessControl",
  "provider": "App\\Modules\\System\\AccessControl\\ServiceProvider",
  "dependencies": [],
  "permission_source": "permissions.php",
  "config_source": "module.php"
}
```

`module.json` adalah manifest deklaratif tunggal. `module.php` digunakan sebagai
runtime configuration source dan dihasilkan oleh profile `default-v1` agar
`config_source` selalu valid. Field wajib adalah
`schema_version`, `name`, `domain`, `namespace`, `version`, `status`, `path`,
`provider`, `dependencies`, `permission_source`, dan `config_source`. Status
manifest hanya boleh `enabled` atau `disabled`; state runtime `Discovered`,
`Validated`, `Registered`, `Running`, dan `Invalid` dimiliki registry, bukan
manifest.

### `permissions.php`

Permission identity dimiliki module:

```php
return [
    [
        'key' => 'access_control.role.manage',
        'description' => 'Manage roles',
        'module' => 'AccessControl',
        'sensitive' => false,
    ],
    [
        'key' => 'access_control.permission.manage',
        'description' => 'Manage permissions',
        'module' => 'AccessControl',
        'sensitive' => true,
    ],
]; 
```

AccessControl melakukan discovery dan sync permission dari module valid; owner
permission tidak berpindah ke AccessControl. Setiap item wajib memiliki `key`,
`description`, `module`, dan `sensitive`; key bersifat unik secara global.

### Module README

Profile `default-v1` menghasilkan `README.md` minimum. README tersebut belum
dianggap context pack lengkap sampai module owner menambahkan boundary, contract,
permission, konfigurasi, test, dan operational notes.

## Generated Code Rules

- Strict typing dan namespace benar.
- ULID migration/model/factory/fixture konsisten.
- Tidak menghasilkan business logic palsu.
- Cross-module dependency hanya contract/communication layer.
- Event memakai typed payload dan correlation ID.
- Security-sensitive output memiliki policy, permission, dan audit hook placeholder.
- Permission identity berada pada `permissions.php` module.
- Generated code tidak menggunakan Wayfinder atau Laravel Boost.

## Conflict and Safety Rules

1. Validate environment dan forbidden dependency.
2. Validate input, target path, namespace, manifest, dan dependency.
3. Build planned file list.
4. Jika conflict dan tidak ada `--force`, berhenti tanpa perubahan.
5. `--dry-run` hanya menampilkan plan.
6. `--force` mencatat file yang ditimpa dan meminta confirmation interaktif;
   CI wajib memakai `--force --yes`.
7. Generator menulis ke staging directory dan melakukan atomic promotion setelah
   seluruh output tervalidasi.
8. Jika staging gagal, temporary output dihapus tanpa menyentuh target.
9. Jika promotion gagal, operation log menjalankan cleanup untuk file yang dibuat;
   overwrite memiliki backup/restore strategy.
10. Output tidak pernah menampilkan secret.

Generator tidak boleh otomatis uninstall dependency forbidden. Generator
menampilkan diagnostic, remediation command yang aman, dan menghentikan proses.

## Output Contract

Default output human-readable. `--json` wajib memiliki:

```json
{
  "success": true,
  "code": "MODULE_CREATED",
  "message": "Module created successfully.",
  "data": {
    "module": "AccessControl",
    "path": "app/Modules/System/AccessControl",
    "created": []
  },
  "diagnostics": []
}
```

Error memiliki exit code non-zero, code stabil, message actionable, dan
diagnostics tanpa sensitive payload.

## Starter Kit Validation

Sebelum generate, verifikasi PHP 8.4+, Laravel 13, React starter kit, Inertia,
React, TypeScript, Vite, Tailwind, shadcn/ui, PostgreSQL, Redis, Ziggy, approved
Spatie package, serta absensi Wayfinder/Laravel Boost.

## Existing Module Inventory

Sebelum `module:make`, agent atau command wrapper wajib menjalankan inventory:

```bash
php artisan module:discover --json
php artisan module:validate --json
php artisan module:list --json
```

Inventory minimal mencatat module name, domain, path, namespace, manifest
version, status, provider, dependencies, permission source, validation result,
dan ownership evidence. Jika command belum tersedia, agent wajib melakukan
read-only scan pada `app/Modules` dan melaporkan keterbatasannya. Generator
menolak membuat module jika target name, path, atau namespace sudah dimiliki
module valid, kecuali caller menggunakan mode extension yang eksplisit.

## Generator Test Requirements

- Naming/namespace validation.
- Manifest and permission schema.
- Dry-run no-write.
- Conflict no-write.
- Force overwrite.
- Partial write cleanup.
- JSON output contract.
- Generated structure snapshot.
- ULID migration output.
- Module discovery/validation integration.
- Forbidden dependency failure.

## Future Enhancements

Feature, resource, CRUD, API, dashboard, report, workflow, dan multi-tenant
generator hanya ditambahkan melalui specification, profile version, test, dan
ADR yang sesuai.

## Open Decisions

## References

- `02.04-SYSTEM-DESIGN.md`
- `03.01-CODING-STANDARD.md`
- `03.02-CONSOLE-COMMANDS.md`
- `03.04-FOLDER-STRUCTURE.md`
- `03.07-MODULES.md`
- `03.08-STUB-SPEC.md`
- `03.10-TEST-PLAN.md`
